---
language: python
before_install:
  - "sudo apt update -qq"
  - "sudo apt install python3-apt"
install:
  - "pip install ansible apt apt_pkg"
  - "printf '[defaults]\nroles_path = ../' > ansible.cfg"
script:
  - "ansible-playbook -v pc.yml"
